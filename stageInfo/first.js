// in first stage we will work on error handling and code
// validation

/*

function isValidPython(code) {
  const pythonCodeRegex = /^[a-zA-Z0-9\s\+\-\*\/%=\(\),;\[\]\{\}_]+$/;

  return pythonCodeRegex.test(code);
}

So in a way we will try to improve above function
pyCheck file is the main location where we work on this stage






*/
